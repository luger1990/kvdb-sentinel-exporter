# 开发环境配置示例
# 复制此文件为.env进行本地配置

# 是否启用调试模式（启用调试工具栏和热重载）
DEBUG=true

# 应用配置
HOST=127.0.0.1
PORT=16379

# 配置文件路径
CONFIG_PATH=config.yaml

# Flask会话密钥（生产环境请设置为随机长字符串）
SECRET_KEY=change-me-in-production

# Redis密码示例，config.yaml 可通过 ${ENV_NAME} 引用
REDIS_GROUP_1_PASSWORD=
KVROCKS_GROUP_1_PASSWORD=
